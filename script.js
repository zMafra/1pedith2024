// Matrix, LIVRE
// A Teia, 16
// Abigail, 16
// Feios, 14
// A liga, LIVRE
function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(200);
}
function setup() {
  background(200);
  let redomendacao = "Matrix";
  text (recomendacao, width / 2, height / 2);
}
function geraREcomendacao(idade) {}
  if(idade >= 10) {
  return "Matriz";
} else {
  return "a Liga";
}
